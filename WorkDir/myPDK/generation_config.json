{
  "generation_params": {
    "comment": "Parameters for random block generation",
    "length_range": {
      "description": "Length quantization step",
      "step": 0.01,
      "comment": "Length will be snapped to this step (not grid)"
    },
    "width_range": {
      "description": "Width quantization step",
      "step": 0.05,
      "comment": "Width will be snapped to this step (not grid)"
    },
    "multiplier_range": {
      "min": 2,
      "max": 8,
      "comment": "Even numbers only, range: [min, max]"
    },
    "num_fingers_range": {
      "min": 2,
      "max": 4,
      "comment": "Number of fingers per transistor"
    },
    "aspect_ratio": {
      "min_aspect_min": 0.2,
      "min_aspect_max": 0.8,
      "max_aspect_min": 1.2,
      "max_aspect_max": 2.0,
      "comment": "Random ranges for min and max aspect ratios"
    }
  },
  "rotation_variants": {
    "comment": "Rotation variant generation. prob_90 < prob_180. Set to 0.0 to disable. deduplicate_square skips rotation when |W-H| < grid.",
    "prob_180": 0.0,
    "prob_90": 0.0,
    "deduplicate_square": true
  },
  "design_constraints": {
    "comment": "Design-specific size constraints for each device type (designer-adjustable)",
    "nmos_rvt": {
      "L": {"min": 0.2, "max": 1.0, "comment": "Length range for NMOS regular-Vt (LV)"},
      "W": {"min": 0.5, "max": 10.0, "comment": "Width range for NMOS regular-Vt"}
    },
    "nmos_lvt": {
      "L": {"min": 0.2, "max": 1.0, "comment": "Length range for NMOS low-Vt (LV) — shorter L allowed for speed"},
      "W": {"min": 0.5, "max": 10.0, "comment": "Width range for NMOS low-Vt"}
    },
    "nmos_hvt": {
      "L": {"min": 0.3, "max": 2.0, "comment": "Length range for NMOS high-Vt (HV, power stages)"},
      "W": {"min": 0.5, "max": 15.0, "comment": "Width range for NMOS high-Vt"}
    },
    "pmos_rvt": {
      "L": {"min": 0.2, "max": 1.0, "comment": "Length range for PMOS regular-Vt (LV)"},
      "W": {"min": 0.5, "max": 10.0, "comment": "Width range for PMOS regular-Vt"}
    },
    "pmos_lvt": {
      "L": {"min": 0.2, "max": 1.0, "comment": "Length range for PMOS low-Vt (LV)"},
      "W": {"min": 0.5, "max": 10.0, "comment": "Width range for PMOS low-Vt"}
    },
    "pmos_hvt": {
      "L": {"min": 0.3, "max": 2.0, "comment": "Length range for PMOS high-Vt (HV, power stages)"},
      "W": {"min": 0.5, "max": 15.0, "comment": "Width range for PMOS high-Vt"}
    },
    "res_poly": {
      "L": {"min": 1.0, "max": 10.0, "comment": "Length range for polysilicon resistor (determines R value)"},
      "W": {"min": 0.5, "max": 5.0, "comment": "Width range for polysilicon resistor"}
    },
    "cap_mom": {
      "L": {"min": 1.0, "max": 10.0, "comment": "Length range for MOM capacitor"},
      "W": {"min": 1.0, "max": 10.0, "comment": "Width range for MOM capacitor"}
    }
  },
  "netlist": {
    "density": 1.2,
    "pins_per_block_min": 3,
    "pins_per_block_max": 6,
    "comment": "Netlist generation: N_nets = density * N_blocks, pins per block 3-6"
  },
  "symmetry": {
    "pair_probability": 0.4,
    "self_sym_probability": 0.2,
    "comment": "Fraction of same-type block pairs that become symmetry pairs. self_sym_probability: chance a leftover same-type block becomes self-symmetric."
  },
  "validation": {
    "max_generation_attempts": 10,
    "comment": "Maximum attempts to generate a block with at least one valid variant"
  },
  "power_nets": {
    "coverage_min": 0.8,
    "comment": "Minimum fraction of blocks connected to VDD or VSS. Each block connects to at most one power rail via pin P0."
  }
}
